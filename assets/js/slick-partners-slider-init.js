jQuery(document).ready(function ($) {
    $('.partners-slider').slick({
        dots: false,
        arrows: false,
        infinite: true,
        autoplay: true,
        autoplaySpeed: 3000,
        // centerMode: true,
        speed: 600,
        // slidesToShow: 1,
        // slidesToScroll: 1,
        variableWidth: true,
    });
});
